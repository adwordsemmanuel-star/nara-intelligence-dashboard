---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={["flex items-center", className]}>
  <img src={`${import.meta.env.BASE_URL}images/nara-logo.png`} alt="NARA Logo" class="h-12 w-auto object-contain" />
</div>

